import React from "react";
import { Link } from "react-router-dom";

function PassButton() {
  return (
    <Link to="pass">
      <div>Pass Button</div>
    </Link>
  );
}

export default PassButton;
